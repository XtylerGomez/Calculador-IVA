#ifndef converter_hpp
#define converter_hpp

#include <iostream>
#include <string>

using namespace std;

class converter {
public:
	void ayuda();
	void suma(int cantidad);
	void resta(int cantidad);
	void checkRegalos(int cantidad);
};

#endif